{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capabilities for the main window - principle of least privilege",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "opener:allow-default-urls"
  ]
}
